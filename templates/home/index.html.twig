{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}


    <section>
        <div class="container">

            {% for item in Products %}

            <div class="row d-flex justify-content-around">

                <div class="col mt-4 py-5">
                    <div>
                        <h3 class="product_title"> {{ item.titre }}</h3>
                    </div>

                    <br>
                    
                    <div class="mt-3 ">
                        <a href="" class="btn bg-dark text-white btn-block btn-lg">Découvrir</a>
                    </div>


                </div>

                <div class="col">
                    <img src= {{ item.image }}" class="img-fluid img-products" alt="image">
                </div>
            </div>

            {% endfor %}
        </div>
    </section>

{% endblock %}
